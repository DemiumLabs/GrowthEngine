import { TaggerCountPipe } from './tagger-count.pipe';

describe('TaggerCountPipe', () => {
  it('create an instance', () => {
    const pipe = new TaggerCountPipe();
    expect(pipe).toBeTruthy();
  });
});
